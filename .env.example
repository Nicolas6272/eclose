# Copy to .env.local with your real keys (never commit .env.local).
# The app loads .env.local as a bundled asset — just `flutter run`.

SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_PUBLISHABLE_KEY
